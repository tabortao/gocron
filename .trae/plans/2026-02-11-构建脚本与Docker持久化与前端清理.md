# 任务计划：构建脚本、Docker 持久化、前端清理（2026-02-11）

## 目标

- 使打包脚本完全适配纯 Go SQLite 驱动，默认禁用 CGO。
- 修复 Docker 升级后 SQLite 数据库未持久化的问题。
- 梳理前端使用 pnpm 时可清理的无用文件清单。

## 执行步骤

1. 修改 package.sh 的 CGO 逻辑（gocron 与 gocron-node 默认 CGO_ENABLED=0）。
2. 更新 docker-compose.yml，显式设置 SQLite 路径到挂载目录（/.gocron/data/gocron.db），确保持久化。
3. 盘点 web/vue 目录下与 Yarn 相关的冗余文件，并给出处理建议。

## 验证

- 构建：本地执行打包脚本进行交叉编译干跑验证（不实际运行）。
- Docker：根据环境变量与挂载路径一致性检查数据文件是否出现在宿主机 ./data/data/gocron.db。
- 前端：确认仅保留 pnpm 锁文件，移除 Yarn 锁文件与脚本或改为 pnpm。
